<h1>My Stuff</h1><%= @profile.name %> | <%= link_to 'Change my profile', edit_profile_path(@profile) %> <br \><%= @profile.user.email %> | <%= link_to 'Change my email or password', edit_user_path(current_user) %> <br \><br \><%= "When you add events to your calendar or Live Giraffe in general, you will see them here." unless @profile.events.any? || @profile.user.events.any? %><% if @profile.events.any? %>  <h2> My Upcoming Events </h2>  <% render @profile.events %><% end %><% if @profile.user.events.any? %>  <h2> Events I created</h2>  <%= render @profile.user.events  %><% end %>